config:
  debug: false

  host: "0.0.0.0"
  port: 80
  secret-key: "key"

  methods:
    NuVotifier:
      enabled: false
    СommandRun:
      enabled: false
      commands: []
    MySqlRequests:
      database: "mineserv"
      username: "mineserv"
      password: "mineserv"
      sqlQuery: []